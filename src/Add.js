import React from "react"

class Add extends React.Component {
    render(){
        return (
            <div>
                <a href="#" className="btn btn-primary btn-lg active" role="button" aria-pressed="true">Add</a>
            </div>

        );
    }
}
export default Add;
